VITE_PORT=3000
SERVER_PORT=3001

# Default xMoney credentials used to prefill the demo so anyone can test it
# without entering their own keys. Use TEST credentials only (pk_test_ / sk_test_).
# These are exposed to the browser (VITE_ prefix), so never put live keys here.
VITE_DEFAULT_SITE_ID=
VITE_DEFAULT_PUBLIC_KEY=
VITE_DEFAULT_SECRET_KEY=